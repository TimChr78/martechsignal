# Double-slash collapse rules removed 2026-09-09: `/*//  https://martechsignal.com/:splat  301`
# matched EVERY single-trailing-slash path (not just double-slash), producing a
# 301 -> 308 infinite loop site-wide. Do not re-add without testing a normal
# trailing-slash URL (e.g. /tools/nocobase/) returns 200 first.
